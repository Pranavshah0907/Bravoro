// Landing page variant switcher — change the import below to switch between designs:
//   Auth      → beams + rotating words hero (currently active)
//   LandingV2 → Mysh-inspired, teal palette, marquee strip
export { default } from "./Auth";
